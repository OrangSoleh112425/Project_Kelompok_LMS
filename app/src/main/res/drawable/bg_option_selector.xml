<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- State when the view is selected (we will manage this manually) -->
    <item android:state_selected="true">
        <shape android:shape="rectangle">
            <solid android:color="#D2E6FF" /> <!-- menu_button_blue -->
            <corners android:radius="12dp" />
            <stroke android:width="2dp" android:color="#1E5BBD" /> <!-- dark_blue_header -->
        </shape>
    </item>

    <!-- Default state -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@android:color/transparent" />
            <corners android:radius="12dp" />
            <stroke android:width="1dp" android:color="#CCCCCC" /> <!-- Light gray border -->
        </shape>
    </item>

</selector>
