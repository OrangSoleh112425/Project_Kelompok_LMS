<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Background Gradient -->
    <item>
        <shape android:shape="rectangle">
            <gradient
                android:angle="270"
                android:startColor="#5D9FFF"
                android:endColor="#1E5BBD"
                android:type="linear" />
            <corners android:bottomLeftRadius="32dp" android:bottomRightRadius="32dp" />
        </shape>
    </item>

    <!-- Decorative Circle (top right) - Resized -->
    <item
        android:width="120dp"
        android:height="120dp"
        android:gravity="top|end"
        android:right="-50dp"
        android:top="-50dp">
        <shape android:shape="oval">
            <solid android:color="#20FFFFFF" /> <!-- White with low opacity -->
        </shape>
    </item>

</layer-list>